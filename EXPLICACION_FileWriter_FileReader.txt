╔════════════════════════════════════════════════════════════════════════════════════╗
║          📚 EXPLICACIÓN DETALLADA: FileWriter, FileReader y BufferedReader          ║
║                  Escritura y Lectura de Archivos en Java - PASO A PASO              ║
╚════════════════════════════════════════════════════════════════════════════════════╝


═══════════════════════════════════════════════════════════════════════════════════════
🎯 PARTE 1: ESCRITURA DE ARCHIVOS CON FileWriter
═══════════════════════════════════════════════════════════════════════════════════════

📌 FUNCIÓN ORIGINAL (líneas 233-270):
─────────────────────────────────────────────────────────────────────────────────────

private static void escribirEnArchivo(String nombre, boolean append) {
    try {
        String rutaCompleta = CARPETA + nombre + ".txt";        // 📍 Paso 1: Construir ruta
        File archivo = new File(rutaCompleta);

        if (!archivo.exists()) {                                 // ✅ Paso 2: Verificar existencia
            System.out.println("❌ El archivo no existe. Créalo primero.");
            return;
        }

        FileWriter escritor = new FileWriter(rutaCompleta, append); // 📝 Paso 3: Abrir archivo
        
        if (!append) {
            String fecha = new SimpleDateFormat("dd/MM/yyyy HH:mm:ss").format(new Date());
            escritor.write("=== Documento creado: " + fecha + " ===\n\n");
        } else {
            escritor.write("\n\n--- Contenido añadido ---\n");
        }

        String linea;
        while (true) {
            linea = sc.nextLine();                               // 📥 Paso 4: Leer del usuario
            if (linea.equalsIgnoreCase("FIN")) {
                break;
            }
            escritor.write(linea + "\n");                        // ✍️ Paso 5: Escribir en archivo
        }

        escritor.close();                                         // 🔐 Paso 6: CERRAR (¡MUY IMPORTANTE!)
        System.out.println("\n✅ Contenido guardado exitosamente.");

    } catch (IOException e) {                                    // ❌ Paso 7: Manejar errores
        System.out.println("❌ Error al escribir en el archivo: " + e.getMessage());
        e.printStackTrace();
    }
}


═══════════════════════════════════════════════════════════════════════════════════════
📝 PASO 1: CONSTRUIR LA RUTA COMPLETA
═══════════════════════════════════════════════════════════════════════════════════════

String rutaCompleta = CARPETA + nombre + ".txt";

Ejemplo:
────────
• CARPETA = "DocumentosPDF/"
• nombre = "apuntes"
• rutaCompleta = "DocumentosPDF/" + "apuntes" + ".txt"
• Resultado final: "DocumentosPDF/apuntes.txt"

¿Por qué hacer esto?
   • FileWriter necesita la ruta COMPLETA del archivo
   • Sin esto, no sabe dónde crear/escribir el archivo


═══════════════════════════════════════════════════════════════════════════════════════
✅ PASO 2: VERIFICAR SI EL ARCHIVO EXISTE
═══════════════════════════════════════════════════════════════════════════════════════

File archivo = new File(rutaCompleta);

if (!archivo.exists()) {
    System.out.println("❌ El archivo no existe. Créalo primero.");
    return;
}

¿Qué sucede aquí?
1. Creamos un objeto File que apunta a "DocumentosPDF/apuntes.txt"
2. archivo.exists() devuelve:
   • true  → El archivo EXISTE
   • false → El archivo NO EXISTE

3. !archivo.exists() significa:
   • ! = NOT (negación)
   • Si devuelve false (no existe), mostramos error y salimos


═══════════════════════════════════════════════════════════════════════════════════════
📝 PASO 3: ABRIR EL ARCHIVO CON FileWriter - ¡LO MÁS IMPORTANTE!
═══════════════════════════════════════════════════════════════════════════════════════

FileWriter escritor = new FileWriter(rutaCompleta, append);

🔧 ¿QUÉ ES FileWriter?
────────────────────────────────────────────────────────────────────────────────────────
FileWriter es una CLASE que permite escribir texto en archivos.

Piénsalo así:
• File → Representa el archivo (como un "icono" en tu computadora)
• FileWriter → Es la "herramienta" para escribir en ese archivo


🔑 PARÁMETROS DE FileWriter:
────────────────────────────────────────────────────────────────────────────────────────

1️⃣ PRIMER PARÁMETRO: rutaCompleta (String)
   La ruta del archivo donde queremos escribir
   Ejemplo: "DocumentosPDF/apuntes.txt"

2️⃣ SEGUNDO PARÁMETRO: append (boolean)
   ────────────────────────────────────────────────────────────────────
   append = true  →  MODO ADICIÓN (APPEND)
   ────────────────────────────────────────────────────────────────────
   Situación:
   El archivo APUNTES.txt contiene:
   ┌─────────────────────────┐
   │ Línea 1: Java es...     │
   │ Línea 2: Es rápido...   │
   └─────────────────────────┘

   Si opens con append = true y escribes "Línea 3: Es orientado a objetos":
   
   Resultado FINAL:
   ┌─────────────────────────┐
   │ Línea 1: Java es...     │
   │ Línea 2: Es rápido...   │
   │ Línea 3: Es orientado   │  ← NUEVO CONTENIDO AÑADIDO
   └─────────────────────────┘

   ✅ El contenido ANTERIOR se MANTIENE
   ✅ El NUEVO contenido se AÑADE AL FINAL


   ────────────────────────────────────────────────────────────────────
   append = false  →  MODO SOBRESCRITURA
   ────────────────────────────────────────────────────────────────────
   Situación:
   El archivo APUNTES.txt contiene:
   ┌─────────────────────────┐
   │ Línea 1: Java es...     │
   │ Línea 2: Es rápido...   │
   └─────────────────────────┘

   Si opens con append = false y escribes "Línea 1: Python es...":
   
   Resultado FINAL:
   ┌─────────────────────────┐
   │ Línea 1: Python es...   │  ← REEMPLAZÓ TODO
   └─────────────────────────┘

   ❌ TODO EL CONTENIDO ANTERIOR SE ELIMINA
   ✅ Se escribe SOLO lo nuevo


📊 EJEMPLO VISUAL DE append:
─────────────────────────────────────────────────────────────────────────

Archivo inicial: "apuntes.txt"
┌────────────────────────┐
│ Introducción a Java    │
│ Tema: Variables        │
└────────────────────────┘

OPCIÓN 1: new FileWriter("apuntes.txt", true)  [append = true]
└─> Escribes: "Tema: Arrays"
    
    Resultado:
    ┌────────────────────────┐
    │ Introducción a Java    │
    │ Tema: Variables        │
    │ Tema: Arrays           │  ← AÑADIDO
    └────────────────────────┘

OPCIÓN 2: new FileWriter("apuntes.txt", false)  [append = false]
└─> Escribes: "Introducción a Python"
    
    Resultado:
    ┌────────────────────────┐
    │ Introducción a Python  │  ← SOBRESCRITO
    └────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════════════
✍️ PASO 4 Y 5: ESCRIBIR CONTENIDO EN EL ARCHIVO
═══════════════════════════════════════════════════════════════════════════════════════

// Si es NUEVA escritura (sobrescritura), añade cabecera con fecha
if (!append) {
    String fecha = new SimpleDateFormat("dd/MM/yyyy HH:mm:ss").format(new Date());
    escritor.write("=== Documento creado: " + fecha + " ===\n\n");
    // Ejemplo de salida:
    // === Documento creado: 20/02/2025 14:30:45 ===
}

// Si es ADICIÓN, añade separador
else {
    escritor.write("\n\n--- Contenido añadido ---\n");
    // Añade líneas en blanco y un separador
}

// BUCLE para leer contenido del usuario
String linea;
while (true) {
    linea = sc.nextLine();                  // 📥 Lee línea del usuario
    if (linea.equalsIgnoreCase("FIN")) {    // ✅ ¿Usuario escribió "FIN"?
        break;                              // Sí → Salir del bucle
    }
    escritor.write(linea + "\n");           // ✍️ Escribir línea en archivo
}

📖 ¿CÓMO FUNCIONA EL BUCLE?
─────────────────────────────────────────────────────────────────────────

Iteración 1:
│ Scanner: "Escribe línea 1:"
│ Usuario escribe: "Java es un lenguaje de programación"
│ ├─ linea = "Java es un lenguaje de programación"
│ ├─ ¿Es "FIN"? NO
│ └─> escritor.write("Java es un lenguaje de programación\n")
│
├─ El archivo ahora contiene:
│  ┌──────────────────────────────────────────┐
│  │ === Documento creado: 20/02/2025 14:... │
│  │ Java es un lenguaje de programación     │
│  └──────────────────────────────────────────┘

Iteración 2:
│ Scanner: "Escribe línea 2:"
│ Usuario escribe: "Fue creado en 1995"
│ ├─ linea = "Fue creado en 1995"
│ ├─ ¿Es "FIN"? NO
│ └─> escritor.write("Fue creado en 1995\n")
│
├─ El archivo ahora contiene:
│  ┌──────────────────────────────────────────┐
│  │ === Documento creado: 20/02/2025 14:... │
│  │ Java es un lenguaje de programación     │
│  │ Fue creado en 1995                      │
│  └──────────────────────────────────────────┘

Iteración 3:
│ Scanner: "Escribe línea 3:"
│ Usuario escribe: "FIN"
│ ├─ linea = "FIN"
│ ├─ ¿Es "FIN"? SÍ
│ └─> break (salir del bucle)

✅ El bucle terminó. Ahora podemos cerrar el archivo.


═══════════════════════════════════════════════════════════════════════════════════════
🔐 PASO 6: CERRAR EL ARCHIVO - ¡¡¡MUY IMPORTANTE!!!
═══════════════════════════════════════════════════════════════════════════════════════

escritor.close();

⚠️ ¿POR QUÉ DEBES CERRAR EL ARCHIVO?
────────────────────────────────────────────────────────────────────────

Imagina que FileWriter es una "llave" que abre el archivo:

┌─────────────────────────────────────┐
│          ARCHIVO EN DISCO            │
│  ┌──────────────────────────────┐   │
│  │ Contenido del archivo...     │   │
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
            ▲
            │ FileWriter abierto
            │ (archivo "bloqueado")
            │
        MEMORIA RAM
        [escritor]


Si NO cierras el archivo:
❌ El archivo sigue "abierto" (bloqueado)
❌ Otros programas NO pueden acceder a él
❌ El contenido podría NO guardarse correctamente
❌ Usas recursos innecesarios de la computadora

Si SÍ cierras el archivo:
✅ Se guaran TODOS los cambios
✅ Se libera la memoria
✅ Otros programas pueden usar el archivo
✅ Todo está bien


═══════════════════════════════════════════════════════════════════════════════════════
🎯 PARTE 2: LECTURA DE ARCHIVOS CON FileReader Y BufferedReader
═══════════════════════════════════════════════════════════════════════════════════════

📌 FUNCIÓN ORIGINAL (líneas 287-331):
─────────────────────────────────────────────────────────────────────────────────────

private static void leerArchivo(String nombre) {
    try {
        String rutaCompleta = CARPETA + nombre + ".txt";        // 📍 Paso 1: Ruta
        File archivo = new File(rutaCompleta);

        if (!archivo.exists()) {                                 // ✅ Paso 2: Verificar
            System.out.println("❌ El archivo no existe.");
            return;
        }

        FileReader lector = new FileReader(rutaCompleta);       // 📖 Paso 3: Abrir lectura
        BufferedReader buffer = new BufferedReader(lector);     // 📥 Paso 4: Buffer

        System.out.println("\n" + repetir('─', 60));
        System.out.println("📖 CONTENIDO DEL ARCHIVO: " + nombre + ".txt");
        System.out.println(repetir('─', 60) + "\n");

        String linea;
        int numeroLinea = 1;
        while ((linea = buffer.readLine()) != null) {           // 🔁 Paso 5: Leer líneas
            System.out.println(numeroLinea + " | " + linea);
            numeroLinea++;
        }

        buffer.close();                                          // 🔐 Paso 6: Cerrar buffer
        lector.close();                                          // 🔐 Cerrar lector

        System.out.println("\n" + repetir('─', 60) + "\n");

    } catch (IOException e) {                                    // ❌ Manejar errores
        System.out.println("❌ Error al leer el archivo: " + e.getMessage());
        e.printStackTrace();
    }
}


═══════════════════════════════════════════════════════════════════════════════════════
📖 PASO 1 Y 2: RUTA Y VERIFICACIÓN (IGUAL QUE ESCRITURA)
═══════════════════════════════════════════════════════════════════════════════════════

String rutaCompleta = CARPETA + nombre + ".txt";
File archivo = new File(rutaCompleta);

if (!archivo.exists()) {
    System.out.println("❌ El archivo no existe.");
    return;
}

Igual que antes:
• Construimos la ruta completa
• Verificamos que el archivo exista
• Si no existe, mostramos error y salimos


═══════════════════════════════════════════════════════════════════════════════════════
📖 PASO 3: FileReader - LEER CARACTERES DEL ARCHIVO
═══════════════════════════════════════════════════════════════════════════════════════

FileReader lector = new FileReader(rutaCompleta);

🔧 ¿QUÉ ES FileReader?
────────────────────────────────────────────────────────────────────────────────────────
FileReader es una CLASE que permite LEER archivos CARÁCTER POR CARÁCTER.

Archivo: "apuntes.txt"
┌────────────────────────────────────────────────┐
│ Java es un lenguaje de programación            │
│ Fue creado en 1995                             │
└────────────────────────────────────────────────┘

FileReader lo lee así:
'J' 'a' 'v' 'a' ' ' 'e' 's' ' ' 'u' 'n' ... '\n' 'F' 'u' 'e' ...
│   │   │   │   │   │   │   │   │   │             │   │   │   │
└─── UNO POR UNO, CARÁCTER POR CARÁCTER ───────────────────────────┘

Problema:
❌ Leer carácter por carácter es MUY LENTO
❌ Si el archivo tiene 10,000 caracteres, hace 10,000 operaciones

📊 COMPARACIÓN:

FileReader (SIN BUFFER):
┌────────────────────────────────────────────────────────────────┐
│ Archivo                                                        │
│ "Java es un lenguaje\n Fue creado en 1995\n..."               │
└────────────────────────────────────────────────────────────────┘
            ▲
            │ Lee: 'J'
            │ Vuelve al disco
            │ Lee: 'a'
            │ Vuelve al disco
            │ Lee: 'v'   ← INEFICIENTE, MUCHAS OPERACIONES
            │ Vuelve al disco
            │ ...
        DISCO DURO


BufferedReader (CON BUFFER):
┌────────────────────────────────────────────────────────────────┐
│ Archivo                                                        │
│ "Java es un lenguaje\n Fue creado en 1995\n..."               │
└────────────────────────────────────────────────────────────────┘
            ▲
            │ Lee MUCHOS caracteres de una (ejemplo: 1024 bytes)
            │ Los almacena en MEMORIA (buffer)
            │ Después lee desde la memoria
            │ ← EFICIENTE, MENOS OPERACIONES


═══════════════════════════════════════════════════════════════════════════════════════
📥 PASO 4: BufferedReader - LEER LÍNEAS COMPLETAS (¡MUCHO MEJOR!)
═══════════════════════════════════════════════════════════════════════════════════════

BufferedReader buffer = new BufferedReader(lector);

🔧 ¿QUÉ ES BufferedReader?
────────────────────────────────────────────────────────────────────────────────────────
BufferedReader envuelve FileReader y permite leer LÍNEA POR LÍNEA en lugar de carácter
por carácter. Es MUCHO más eficiente.

Piénsalo así:
• FileReader = Lee letra por letra
• BufferedReader = Lee línea completa

Archivo:
┌────────────────────────────────────────────────────────────────┐
│ Línea 1: Java es un lenguaje de programación                   │
│ Línea 2: Fue creado en 1995                                    │
│ Línea 3: Es orientado a objetos                                │
└────────────────────────────────────────────────────────────────┘

Con BufferedReader:
┌─────────────────────────────────────────────────────────────────┐
│ buffer.readLine() devuelve:                                     │
│ "Java es un lenguaje de programación"       (Línea 1 COMPLETA)  │
│                                                                 │
│ buffer.readLine() devuelve:                                     │
│ "Fue creado en 1995"                        (Línea 2 COMPLETA)  │
│                                                                 │
│ buffer.readLine() devuelve:                                     │
│ "Es orientado a objetos"                    (Línea 3 COMPLETA)  │
│                                                                 │
│ buffer.readLine() devuelve:                                     │
│ null (NO HAY MÁS LÍNEAS)                                        │
└─────────────────────────────────────────────────────────────────┘

Ventaja:
✅ Es perfectamente natural para leer línea por línea
✅ Es MUCHO más rápido
✅ El código es más legible


═══════════════════════════════════════════════════════════════════════════════════════
🔁 PASO 5: LEER LÍNEAS CON BUCLE while
═══════════════════════════════════════════════════════════════════════════════════════

String linea;
int numeroLinea = 1;
while ((linea = buffer.readLine()) != null) {
    System.out.println(numeroLinea + " | " + linea);
    numeroLinea++;
}

📖 ¿CÓMO FUNCIONA?
─────────────────────────────────────────────────────────────────────

while ((linea = buffer.readLine()) != null) {
      │                                 │
      └─────────────────┬────────────────┘
                        │
                        ▼
        ┌──────────────────────────────────┐
        │ ASIGNACIÓN Y CONDICIÓN EN UNO     │
        │                                  │
        │ 1. linea = buffer.readLine()     │
        │    ↓ Lee la siguiente línea      │
        │    ↓ Guarda en variable "linea"  │
        │                                  │
        │ 2. != null                       │
        │ ↓ ¿Es diferente de null?         │
        │ ↓ Si es verdadero, continúa      │
        │ ↓ Si es falso, sale del bucle    │
        └──────────────────────────────────┘

Iteración 1:
│ buffer.readLine() → "Java es un lenguaje"
│ linea = "Java es un lenguaje"
│ ¿!= null? SÍ → Continuar
│ Mostrar: 1 | Java es un lenguaje
│ numeroLinea++  → numeroLinea = 2

Iteración 2:
│ buffer.readLine() → "Fue creado en 1995"
│ linea = "Fue creado en 1995"
│ ¿!= null? SÍ → Continuar
│ Mostrar: 2 | Fue creado en 1995
│ numeroLinea++  → numeroLinea = 3

Iteración 3:
│ buffer.readLine() → "Es orientado a objetos"
│ linea = "Es orientado a objetos"
│ ¿!= null? SÍ → Continuar
│ Mostrar: 3 | Es orientado a objetos
│ numeroLinea++  → numeroLinea = 4

Iteración 4:
│ buffer.readLine() → null (NO HAY MÁS LÍNEAS)
│ linea = null
│ ¿!= null? NO → SALIR DEL BUCLE

Resultado final mostrado:
┌─────────────────────────────────────────────────────────┐
│ ────────────────────────────────────────────────────    │
│ 📖 CONTENIDO DEL ARCHIVO: apuntes.txt                   │
│ ────────────────────────────────────────────────────    │
│                                                         │
│ 1 | Java es un lenguaje de programación                │
│ 2 | Fue creado en 1995                                  │
│ 3 | Es orientado a objetos                              │
│                                                         │
│ ────────────────────────────────────────────────────    │
└─────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════════════
🔐 PASO 6: CERRAR BUFFER Y LECTOR
═══════════════════════════════════════════════════════════════════════════════════════

buffer.close();     // Cierra BufferedReader
lector.close();     // Cierra FileReader

⚠️ ¿POR QUÉ CERRAR AMBOS?
────────────────────────────────────────────────────────────────────────

    Archivo en disco
            ▲
            │ Abierto por FileReader
            │
        ┌───┴────────────────────────┐
        │   FileReader (abierto)      │
        │   ├─ Lee caracteres         │
        │   └─ Accede al archivo      │
        │         ▲                   │
        │         │ Usa              │
        │         │                  │
        │   BufferedReader (abierto)  │
        │   ├─ Lee líneas             │
        │   └─ Usa FileReader         │
        └────────────────────────────┘

Si SOLO cierras BufferedReader:
│ buffer.close()  ✅
│ lector.close()  ❌ (NO LO CIERRAS)
│
└─> FileReader sigue abierto
    El archivo sigue bloqueado
    Desperdicio de recursos

Si cierras AMBOS:
│ buffer.close()  ✅
│ lector.close()  ✅
│
└─> Todo se cierra correctamente
    Archivo disponible para otros programas
    Recursos liberados


═══════════════════════════════════════════════════════════════════════════════════════
📊 RESUMEN VISUAL: FLUJO COMPLETO DE ESCRITURA
═══════════════════════════════════════════════════════════════════════════════════════

START
  ↓
┌────────────────────────────────────────────────────────┐
│ 1. Construir ruta: "DocumentosPDF/apuntes.txt"         │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 2. Verificar que existe                                 │
│    ├─ NO existe? → Mostrar error y SALIR               │
│    └─ SÍ existe? → Continuar                           │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 3. Abrir FileWriter                                     │
│    FileWriter escritor = new FileWriter(ruta, append)  │
│                                                        │
│    La "llave" se abre para escribir                    │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 4. Escribir cabecera (si es nuevo)                      │
│    escritor.write("=== Documento creado: ... ===\n\n") │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 5. Bucle while - Leer del usuario y escribir           │
│    ├─ Lee línea del usuario                           │
│    ├─ ¿Es "FIN"? SÍ → Salir del bucle                 │
│    └─ NO → Escribir en archivo y continuar             │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 6. Cerrar FileWriter                                    │
│    escritor.close()                                    │
│                                                        │
│    La "llave" se cierra                                │
│    Todos los datos se guardan                          │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 7. Mensaje de éxito                                     │
│    "✅ Contenido guardado exitosamente"                │
└────────────────────────────────────────────────────────┘
  ↓
END


═══════════════════════════════════════════════════════════════════════════════════════
📊 RESUMEN VISUAL: FLUJO COMPLETO DE LECTURA
═══════════════════════════════════════════════════════════════════════════════════════

START
  ↓
┌────────────────────────────────────────────────────────┐
│ 1. Construir ruta: "DocumentosPDF/apuntes.txt"         │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 2. Verificar que existe                                 │
│    ├─ NO existe? → Mostrar error y SALIR               │
│    └─ SÍ existe? → Continuar                           │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 3. Abrir FileReader                                     │
│    FileReader lector = new FileReader(ruta)            │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 4. Envolver con BufferedReader                          │
│    BufferedReader buffer = new BufferedReader(lector)  │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 5. Mostrar encabezado                                   │
│    Título: "📖 CONTENIDO DEL ARCHIVO: apuntes.txt"    │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 6. Bucle while - Leer líneas                            │
│    while ((linea = buffer.readLine()) != null)         │
│    ├─ Lee siguiente línea                             │
│    ├─ ¿Es null? SÍ → Salir del bucle                  │
│    └─ NO → Mostrar línea numerada y continuar         │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 7. Cerrar BufferedReader                                │
│    buffer.close()                                      │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 8. Cerrar FileReader                                    │
│    lector.close()                                      │
└────────────────────────────────────────────────────────┘
  ↓
┌────────────────────────────────────────────────────────┐
│ 9. Mostrar línea de separación                          │
└────────────────────────────────────────────────────────┘
  ↓
END


═══════════════════════════════════════════════════════════════════════════════════════
🎯 TABLA COMPARATIVA
═══════════════════════════════════════════════════════════════════════════════════════

┌──────────────────┬──────────────────────┬──────────────────────────────┐
│     ASPECTO      │   ESCRITURA          │        LECTURA               │
├──────────────────┼──────────────────────┼──────────────────────────────┤
│ Clase Principal  │ FileWriter           │ FileReader + BufferedReader  │
├──────────────────┼──────────────────────┼──────────────────────────────┤
│ Parámetros       │ (ruta, append)       │ (ruta)                       │
├──────────────────┼──────────────────────┼──────────────────────────────┤
│ Escribir Datos   │ write(String)        │ readLine()                   │
├──────────────────┼──────────────────────┼──────────────────────────────┤
│ Condición Final  │ Bucle while (true)   │ while ((x = readLine())      │
│                  │ + break              │ != null)                     │
├──────────────────┼──────────────────────┼──────────────────────────────┤
│ Cerrar Recursos  │ close() (1 clase)    │ close() (2 clases)           │
├──────────────────┼──────────────────────┼──────────────────────────────┤
│ Append Parámetro │ true = añadir        │ No existe concepto de append │
│                  │ false = sobrescribir │ en lectura                   │
├──────────────────┼──────────────────────┼──────────────────────────────┤
│ Excepciones      │ IOException          │ IOException                  │
└──────────────────┴──────────────────────┴──────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════════════
💡 EJEMPLO PRÁCTICO COMPLETO
═══════════════════════════════════════════════════════════════════════════════════════

Ejecución del programa:

1️⃣ CREAR ARCHIVO:
   ────────────────────────────────────────────────────────────
   Menú → Opción 1
   Nombre: "miArchivo"
   
   Resultado: Crea "DocumentosPDF/miArchivo.txt" (vacío)


2️⃣ ESCRIBIR EN ARCHIVO:
   ────────────────────────────────────────────────────────────
   Menú → Opción 2
   Nombre: "miArchivo"
   Modo: 1 (Sobrescribir)
   
   El archivo se abre con:
   FileWriter escritor = new FileWriter("DocumentosPDF/miArchivo.txt", false)
   
   Contenido escrito por el usuario:
   > Como usar FileWriter
   > Es para escribir en archivos
   > FIN
   
   Archivo resultante:
   ┌────────────────────────────────────────────────────────┐
   │ === Documento creado: 20/02/2025 14:30:45 ===         │
   │                                                        │
   │ Como usar FileWriter                                   │
   │ Es para escribir en archivos                           │
   └────────────────────────────────────────────────────────┘


3️⃣ LEER ARCHIVO:
   ────────────────────────────────────────────────────────────
   Menú → Opción 3
   Nombre: "miArchivo"
   
   El archivo se abre con:
   FileReader lector = new FileReader("DocumentosPDF/miArchivo.txt")
   BufferedReader buffer = new BufferedReader(lector)
   
   Salida mostrada:
   ┌────────────────────────────────────────────────────────┐
   │ ────────────────────────────────────────────────────  │
   │ 📖 CONTENIDO DEL ARCHIVO: miArchivo.txt              │
   │ ────────────────────────────────────────────────────  │
   │                                                        │
   │ 1 | === Documento creado: 20/02/2025 14:30:45 ===   │
   │ 2 |                                                    │
   │ 3 | Como usar FileWriter                              │
   │ 4 | Es para escribir en archivos                      │
   │                                                        │
   │ ────────────────────────────────────────────────────  │
   └────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════════════
✨ PUNTOS CLAVE PARA RECORDAR
═══════════════════════════════════════════════════════════════════════════════════════

✅ FileWriter:
   • Escribe en archivos
   • Parámetro boolean append: true (añadir) o false (sobrescribir)
   • SIEMPRE cierra con .close()

✅ FileReader + BufferedReader:
   • FileReader lee caracteres (lento)
   • BufferedReader envuelve FileReader (rápido y por líneas)
   • readLine() devuelve null cuando no hay más líneas
   • Cierra AMBOS

✅ Try-Catch:
   • IOException = Error al leer/escribir archivos
   • Siempre maneja excepciones

✅ Métodos útiles:
   • write() = Escribe en archivo
   • readLine() = Lee una línea
   • close() = Cierra el archivo

